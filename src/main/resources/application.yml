server:
  port: 8888

spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Guimathis/spring-cloud-configs
          search-paths: 'greeting-service*'
          # if the repository is private:
         # username:
         # password: